{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block body %}
    <header class="masthead bg-black">
        <div class="container position-relative h-100">
            <div class="row justify-content-center align-content-center h-100">
                <div class="col-xl-6">
                    <div class="text-center text-white">
                        <h1 class="mb-5 home-title">Trouvez LE serveur pour stocker vos données au bon endroit !</h1>
                        <div class="row">
                            <a class="btn btn-outline-light btn-lg" href="{{ path('region_index') }}">Rechercher</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </header>
{% endblock %}
